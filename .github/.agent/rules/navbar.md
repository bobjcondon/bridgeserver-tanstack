---
trigger: model_decision
description: When implementing navbar
---

# Navbar

- add a navbar to the homepage layout.tsx 
- the navbar should have a "Data" pulldown menu
- the "Data" pulldown menu should have an entry for each Table <T> in the database (linking to app/<T>)
- the navbar should have signon button which uses Clerk for signon validation.

